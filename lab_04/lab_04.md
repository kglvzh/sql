# Лабораторная работа 4. Оконные функции для анализа данных

Номер вариант 3

## Цель работы
Изучить концепцию оконных функций в SQL и научиться применять их для выполнения сложных аналитических расчетов: ранжирования, вычисления скользящих средних, нарастающих итогов и сравнительного анализа строк без группировки данных.

## Задачи
- Разобрать синтаксис оконных функций: предложение OVER, PARTITION BY, ORDER BY и ROWS/RANGE.
- Научиться использовать функции ранжирования: ROW_NUMBER(), RANK(), DENSE_RANK(), NTILE().
- Освоить функции смещения для доступа к предыдущим и следующим строкам: LAG(), LEAD().
- Применить агрегатные функции в качестве оконных для вычисления накопительных итогов и скользящих статистик.

---

# Задания

## Задание 4.1: Найти топ-3 самых дорогих товара (rank <= 3) в каждой товарной категории (product_type).


Скрипт для выполнения запроса:

```sql
select product_type, model, base_msrp, rank
from (
	select *, rank() over (partition by product_type order by base_msrp desc) as rank
	from products
)
where rank <= 3
order by product_type, rank;
```

Результат выполнения:

<img width="359" height="155" alt="image" src="https://github.com/user-attachments/assets/c7b444af-062d-4864-81d2-8fcbe4114dec" />


---

## Задание 4.2: Разбить всех клиентов на 4 группы (NTILE) в зависимости от их широты (latitude) — от севера к югу.


Скрипт для выполнения запроса:

```sql
select customer_id, latitude,
ntile(4) over (order by latitude desc) as lat_group
from customers
where latitude is not null and state = 'OK'         -- фильтр по штату для региональной сегминтации клиентов
order by latitude desc, customer_id;
```

**Комментарий:**
- фильтр по штату для региональной сегментации клиентов
- сортировка для наглядности распределения по широте

Результат выполнения:

<img width="293" height="377" alt="image" src="https://github.com/user-attachments/assets/78634d0e-ee98-4a67-b99c-61c19e1afddc" />


---

## Задание 4.3: Рассчитать скользящую сумму продаж (3 дня: вчера, сегодня, завтра) для всей компании.

Скрипт для выполнения запроса:

```sql
select 
	sales_transaction_date::date as sales_date,      -- для группировки по дням без учёта времени
	sum(sales_amount) as daily_total,
	sum(sum(sales_amount)) over (order by sales_transaction_date::date range between interval '1' preceding and current row) as total     -- учитывает календарные интервалы, а не количество строк в выборке
from sales
where dealership_id is not null and sales_transaction_date between '2015-01-01' and '2015-12-31'
									-- для фокусировки на конкретном отчётном периоде
group by sales_transaction_date::date
order by sales_date;
```

**Комментарий:**
- приведение timestamp к дате для группировки продаж за одни сутки
- rows между 1 предыдущей и текущей строкой для скользящей суммы за 3 дня
- фильтр по году для анализа данных за конкретный отчётный период

Результат выполнения:

<img width="323" height="377" alt="image" src="https://github.com/user-attachments/assets/c43dffad-32ee-4bd9-8c84-56fe475c1148" />


---

## Вывод

В результате выполнения лабораторной работы закреплены навыки применения оконных функций SQL для решения аналитических задач. Освоены различные категории оконных функций: функции ранжирования (RANK, NTILE) для выделения топ-товаров по категориям и географической сегментации клиентов, агрегатные функции в качестве оконных для расчета скользящих сумм, а также технику приведения типов данных (::DATE) для корректной группировки по временным периодам. Это позволило решить бизнес-задачи: определить самые дорогие товары в каждой категории, разделить клиентов на группы по географическому признаку и рассчитать скользящую сумму продаж компании. Полученные навыки являются необходимым инструментарием для глубокого анализа данных (Data Analysis) и построения продвинутой аналитической отчетности.

Полный SQL скрипт для выполнения запросов представлен в файле [lab_04.sql](lab_04.sql).
