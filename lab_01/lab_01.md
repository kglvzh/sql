# Лабораторная работа 1: Основы работы с SQL-запросами: выборка и модификация данных

**Номер варианта 3**

## Цель работы
Научиться формировать запросы на выборку данных (SELECT) с использованием фильтрации, сортировки и логических операторов. Освоить операции манипулирования данными (CRUD: Create, Read, Update, Delete) в безопасной локальной среде.


## Задание 1.1: Продавцы, нанятые в 2018 году
Продавцы (salespeople) нанятые в 2018. Сортировка: Фамилия (А-Я)

Скрипт для выполнения запроса

```sql
select
	first_name, -- необходимо для идентификации сотрудника
	last_name,  -- необходимщ для идентификации сотрудника
	gender,     -- для анализа демографического состава команды
	hire_date   -- подтверждение, что сотрудник нанят в 2018 году
from salespeople
where hire_date between '2018-01-01' and '2018-12-31'
order by last_name asc;
```

**Комментарий к выбору полей:**
- `first_name`, `last_name` — необходимы для идентификации сотрудника
- `gender` — для анализа демографического состава команды
- `hire_date` — подтверждение, что сотрудник нанят в 2018 году

**Результат выполнения:**

<img width="462" height="395" alt="image" src="https://github.com/user-attachments/assets/383a11f3-805d-4443-8738-9be294d3fa3f" />


## Задание 1.2: Письма, открытые, но не кликнутые

Скрипт для выполнения запроса

```sql
select 
	email_subject,   -- для анализа контента
	sent_date,       -- для анализа временных паттернов
	opened_date      -- для оценки времени отклика аудитории
from emails
where opened = 't' and clicked = 'f' and sent_date between '2011-01-01' and '2011-01-09'  -- сужение выборки для более детального анализа
order by email_id;
```

**Комментарий к выбору полей:**
- `email_subject` — для анализа контента
- `sent_date` — для анализа временных паттернов
- `opened_date` — для оценки времени отклика аудитории

**Результат выполнения:**

<img width="467" height="381" alt="image" src="https://github.com/user-attachments/assets/b1f5b18b-7957-4e24-bce6-b3b6b1f4232f" />

---

# Вывод
В результате выполнения работы освоены базовые операции выборки данных (SELECT) с применением фильтрации (WHERE) и сортировки (ORDER BY). Полученные навыки позволяют формировать простые, но информативные выгрузки, такие как поиск сотрудников по дате найма или отбор целевой аудитории для маркетинговых кампаний по заданным поведенческим признакам (открытые, но неактивные письма).

Полный SQL скрипт для выполнения запросов представлен в файле [lab_01.sql.](lab_01.sql).
